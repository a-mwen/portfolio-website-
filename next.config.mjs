/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['brintonvision.com', 'pngimg.com'], // Add other domains as needed
    },
  };
  
  export default nextConfig;
  